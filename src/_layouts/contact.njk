{% extends "./base.njk" %}
{% block content %}
    <article> {# class="multi" #}
        {# <form action="contato/envia.php" method="post" name="contactform" id="contactform">
            <h2 class="box__title">Entre em contato conosco</h2>
            <label for="name">*Nome:
            </label>
            <input name="name" id="name" type="text"/>
            <label for="email">*Email:
            </label>
            <input name="email" id="email" type="text"/>
            <label for="tel">*DDD+Telefone:
            </label>
            <input name="tel" id="tel" type="text" maxlength="11"/>
            <label for="subject">*Assunto:
            </label>
            <input name="subject" id="subject" type="text"/>
            <label for="message">*Mensagem:
            </label>
            <textarea name="message" id="message"></textarea>
            <div class="btn-group">
                <input name="cf_submit" type="submit" class="bt btn" id="cf_submit" value="Enviar"/>
                <input name="cf_reset" type="reset" class="bt btn btn--inverse" id="cf_reset" value="Apagar"/>
            </div>
        </form> #}

        <div class="box box--special">
            <h2 class="box__title">{{ title | safe }}</h2>
            
            {{ content | safe }}
        </div>
    </article>
    <article>
        <iframe src="{{ map | safe }}" width="100%" height="450" frameborder="0" style="border:0;" allowfullscreen="" aria-hidden="false" tabindex="0"></iframe>
    </article>
{% endblock %}